machine:
  java:
    version: oraclejdk8
  environment:
    TEST: ci

test:
  post:
    - npm run report-coverage

dependencies:
  override:
    - mvn clean install -DskipTests
  cache_directories:
    - $HOME/.m2/repository

test:
  override:
    - mv target $CIRCLE_ARTIFACTS